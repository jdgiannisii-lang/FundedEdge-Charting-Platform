{
  "name": "@fundededge/rules-engine",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint ./src",
    "lint:fix": "biome lint --write ./src",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@fundededge/config": "workspace:*",
    "typescript": "^6.0.3",
    "vitest": "^2.1.0"
  }
}
